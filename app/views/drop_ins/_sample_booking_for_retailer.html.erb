<div class="row">
  <div class="single-booking col-xs-10 col-xs-offset-1">
    <div class="content">
      <div class="col-xs-7 col-xs-offset-2">
        <h3>Jane (Sample) </h3>
        <h3>Tomorrow @ 11AM</h3>
        <h4>"Need to find something chic for my company's holiday party"</h4>
      </div>
      <div class="col-xs-3">
        <h1>
          <i class="show-preferences-toggle fa fa-caret-right"></i>
        </h1>
      </div>
      <div class="col-xs-8 col-xs-offset-2 preferences-details">
        <h3>Tops</h3>
        <ul>
          <li>Sizes: <%= TopSize.all.sample.name %></li>
          <li>Budget: max $50</li>
          <li>Fit: <%= TopFit.all.sample.name %></li>
        </ul>
        <h3>Bottoms</h3>
        <ul>
          <li>Sizes: <%= BottomSize.all.sample.name %></li>
          <li>Budget: max $150</li>
          <li>Fit: <%= BottomFit.all.sample.name %></li>
        </ul>
        <h3>Dresses</h3>
        <ul>
          <li>Sizes: <%= DressSize.all.sample.name %></li>
          <li>Budget: max $150</li>
        </ul>
        <h3>Build considerations: <%= BodyBuild.all.sample(2).map(&:name).join(', ') %></h3>
        <% rand_shape = BodyShape.all.sample%>
        <h3>Body shape: <%= rand_shape.name%></h3>
        <h4><%= rand_shape.description %></h4>
        <%= image_tag("body_shapes/#{rand_shape.name.downcase.gsub(' ','-')}.jpg",
                      alt: rand_shape.name, class: 'img-responsive')%>
        <h3>Loved looks: </h3>
        <div class="row">
          <% Look.all.sample(3).each do |look|%>
              <div class="col-xs-4">
                <%= image_tag(look.image_path, alt: look.name, class: 'img-responsive') %>
              </div>
          <% end %>
        </div>
        <h3>Values: <%= SpecialConsideration.all.sample(2).map(&:name).join(', ') %></h3>
        <h3>Parts to flaunt: <%= Part.all.sample(2).map(&:name).join(', ') %></h3>
        <h3>Parts to downplay: <%= Part.all.sample.name %></h3>
        <h3>Colors to avoid: <%= Color.all.sample(2).map(&:name).join(', ') %></h3>
      </div>
    </div>
  </div>
</div>