<%= render 'shared/feed', 
      list: current_shopper.favorites.to_a
            .keep_if{|fav| fav.favoriteable.live?}.map{|fav| 
              {object: fav.favoriteable, justification: []}
            },
      tab: 'favorite' %>
