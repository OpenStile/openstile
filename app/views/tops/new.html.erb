<%= provide(:title, 'Create Top') %>
<%= render "shared/header" %>

<%= render 'shared/item_creation_form', object: @top,
                                        size_collection: :top_size_ids,
                                        size_model: TopSize %>

