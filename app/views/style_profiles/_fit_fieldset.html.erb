<div class="body-coverage">
  <div class="row">
    <div class="col-md-6 col-xs-6">
      <h4>What do you like to show off and what do you cover up?</h4>
    </div>
    <div class="col-md-2 col-xs-2 text-center">
      <h4>Keep it covered</h4>
    </div>
    <div class="col-md-2 col-xs-2 text-center">
      <h4>I mix it up</h4>
    </div>
    <div class="col-md-2 col-xs-2 text-center">
      <h4>Flaunt it!</h4>
    </div>
  </div>
  <div class="parts">
    <% Part.all.each do |part| %>
      <div class="row part" id=<%= "part_#{part.id}"%>>
        <div class="col-md-3 col-md-offset-2 col-xs-3 col-xs-offset-2">
          <h4><%= part.name %></h4>
        </div>
        <%= form_helper.fields_for :part_exposure_tolerances, 
            @style_profile.part_exposure_tolerances.find_or_create_by(part_id: part.id) do |tolerance_fields|%>  
          <%= tolerance_fields.hidden_field(:part_id) %>
          <div class="col-md-2 col-md-offset-1 col-xs-2 col-xs-offset-1 text-center">
            <%= tolerance_fields.label(:tolerance_1, "Cover", class: "sr-only") %>
            <%= tolerance_fields.radio_button(:tolerance, 1) %>
          </div>
          <div class="col-md-2 col-xs-2 text-center">
            <%= tolerance_fields.label(:tolerance_5, "Mix it up", class: "sr-only") %>
            <%= tolerance_fields.radio_button(:tolerance, 5) %>
          </div>
          <div class="col-md-2 col-xs-2 text-center">
            <%= tolerance_fields.label(:tolerance_10, "Show off", class: "sr-only") %>
            <%= tolerance_fields.radio_button(:tolerance, 10) %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
